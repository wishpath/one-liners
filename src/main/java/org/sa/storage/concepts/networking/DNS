DNS
Domain Name System — maps user-friendly domain names to numeric IP addresses.
Please explain concept 'DNS': *expand each letter **D? **N? **S? *name the 2 elements that are mapped together
User has to expand each letter correctly (D=Domain, N=Name, S=System). Add points explicitly in the math formula (D=2/10, N=2/10, S=2/10). User has to correctly identify the 2 mapped elements (domain names=2/10, IP addresses=2/10). Add points explicitly in the math formula. Perfect score math formula: 2/10 + 2/10 + 2/10 + 2/10 + 2/10 = 10/10.
networking