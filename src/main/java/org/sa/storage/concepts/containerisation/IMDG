IMDG
In-Memory Data Grid — distributed computing storing data in RAM for fast access
Explain IMDG. *Expand acronym letters in order: **I? **M? **D? **G? *state whether it runs on a single instance or not *name where data is stored *name the performance characteristic
User has to correctly expand each acronym letter in order (I = In, M = Memory, D = Data, G = Grid) — score 4 * 1/10 = 4/10. Each letter is evaluated separately and must match exactly. User has to correctly state that it does not run on a single instance (multiple cooperating instances / distributed behavior) — score 2/10. User has to correctly identify where data is stored (RAM or memory) — score 2/10. User has to correctly identify the performance characteristic (fast access) — score 2/10. Add points explicitly in the math formula. Perfect score math formula: 4 * 1/10 + 2/10 + 2/10 + 2/10 = 10/10.
containerisation